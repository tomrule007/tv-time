{
  "name": "tv-time-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend for TV-TIME Roku app to track daily TV usage and switch to a limit screen.",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.0"
  }
}
